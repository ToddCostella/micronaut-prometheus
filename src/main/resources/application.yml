endpoints:
  prometheus:
    sensitive: false
micronaut:
  application:
    name: microPrometheus
  metrics:
    enabled: true
    export:
      prometheus:
        enabled: true
        descriptions: true
        step: PT1M
